diff --git a/microbrain/neurons/curiosity_drive_neuron.py b/microbrain/neurons/curiosity_drive_neuron.py
--- a/microbrain/neurons/curiosity_drive_neuron.py
+++ b/microbrain/neurons/curiosity_drive_neuron.py
@@ -1,6 +1,7 @@
 from __future__ import annotations

 

 import time

+import re

 from pathlib import Path

 from typing import Iterable

 

@@ -318,16 +319,101 @@
                 "Generate one short sentence of curious babble. "

                 "Keep it under 12 words."

             )

+

+            # If this session has no mimic seed yet, borrow a recent USER turn from persistent memory

+            # so babble can pick up where we left off across restarts.

+            unigrams = await ctx.get_kv("mimic:unigrams", {}) or {}

+            bigrams = await ctx.get_kv("mimic:bigrams", {}) or {}

+            recent_phrases = await ctx.get_kv("mimic:recent_phrases", []) or []

+            last_user_text = (await ctx.get_kv("mimic:last_user_text", "") or "").strip()

+

+            if not last_user_text:

+                mem_store = await ctx.get_kv("memory:store", None)

+                seed_text = ""

+                if mem_store is not None:

+                    try:

+                        epi_hits = mem_store.last_episodic(n=120) or []

+                    except Exception:

+                        epi_hits = []

+

+                    # Pass 1: prefer a "USER:" line with at least 2 tokens (more useful for bigrams)

+                    for it in reversed(epi_hits):

+                        if not isinstance(it, dict):

+                            continue

+                        t = str(it.get("text", "") or "").strip()

+                        if not t or not t.startswith("USER:"):

+                            continue

+                        cand = t.split("USER:", 1)[1].strip()

+                        if not cand or cand.startswith("/"):

+                            continue

+                        tok_count = len(re.findall(r"[A-Za-z0-9']+", cand))

+                        if tok_count >= 2:

+                            seed_text = cand

+                            break

+

+                    # Pass 2: if nothing decent found, accept any non-empty "USER:" line

+                    if not seed_text:

+                        for it in reversed(epi_hits):

+                            if not isinstance(it, dict):

+                                continue

+                            t = str(it.get("text", "") or "").strip()

+                            if not t or not t.startswith("USER:"):

+                                continue

+                            cand = t.split("USER:", 1)[1].strip()

+                            if cand and not cand.startswith("/"):

+                                seed_text = cand

+                                break

+

+                    # Last resort: semantic seed

+                    if not seed_text:

+                        try:

+                            sem_hits = mem_store.search_semantic("user", k=30) or []

+                        except Exception:

+                            sem_hits = []

+                        for it in sem_hits:

+                            if not isinstance(it, dict):

+                                continue

+                            t = str(it.get("text", "") or "").strip()

+                            if not t:

+                                continue

+                            if t.startswith("[echo:") or "Quick check about" in t:

+                                continue

+                            if re.search(r"\b(kill|murder|suicide|harm)\b", t, re.IGNORECASE):

+                                continue

+                            seed_text = t

+                            break

+

+                if seed_text:

+                    last_user_text = seed_text

+                    await ctx.set_kv("mimic:last_user_text", last_user_text)

+

+            # If we still have no n-grams (fresh boot), bootstrap a tiny corpus from the seed text.

+            if last_user_text:

+                toks = re.findall(r"[A-Za-z0-9']+|[.,!?;:]", last_user_text.lower())

+                toks = toks[:24]

+

+                if toks and (not isinstance(unigrams, dict) or not unigrams):

+                    unigrams = {}

+                    for tok in toks:

+                        unigrams[tok] = int(unigrams.get(tok, 0)) + 1

+                    await ctx.set_kv("mimic:unigrams", unigrams)

+

+                if len(toks) >= 2 and (not isinstance(bigrams, dict) or not bigrams):

+                    bigrams = {}

+                    for a, b in zip(toks, toks[1:]):

+                        key = f"{a}|{b}"

+                        bigrams[key] = int(bigrams.get(key, 0)) + 1

+                    await ctx.set_kv("mimic:bigrams", bigrams)

             meta2 = {

                 "boredom_active": True,

                 "allow_babble": True,

                 "source": "curiosity_drive",

                 "boost": boost,

                 "mimic": {

-                    "unigrams": await ctx.get_kv("mimic:unigrams", {}) or {},

-                    "bigrams": await ctx.get_kv("mimic:bigrams", {}) or {},

-                    "recent_phrases": await ctx.get_kv("mimic:recent_phrases", []) or [],

-                    "last_user_text": await ctx.get_kv("mimic:last_user_text", "") or "",

+                    "unigrams": unigrams,

+                    "bigrams": bigrams,

+                    "recent_phrases": recent_phrases,

+                    "last_user_text": last_user_text,

                 },

             }

 

